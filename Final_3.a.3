
model{
  for (i in 1:count) {
    y[i] ~ dlnorm(mu, tau)
  }

  y.pred ~ dlnorm(mu, tau)

  mu ~ dnorm(mu_0, tau_0)
  tau ~ dgamma(nu_0 / 2, nu_0 * sigma20 / 2)

  mu_0 <- 10
  tau_0 <- 25

  nu_0 <- 10
  sigma20 <- 25

  sigma2 <- 1 / tau
}

